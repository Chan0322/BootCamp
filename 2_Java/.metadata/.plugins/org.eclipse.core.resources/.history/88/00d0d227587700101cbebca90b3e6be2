package com.study;

public class study {

	public static void main(String[] args) {	
		// new 연산자로 객체 생성 시, 생성자 호출!
		House h1 = new House();
		House h2 = new House("고급형");
	}
	
	public class Book {
		public Book() {}
		public Book(String title) {
			System.out.println("책 제목은 " + title + "입니다.");
		}
	}
	Book book = new Book();	// 기본 생성자가 자동 생성되지 않아 오류 발생.
	
	
	
	
	
	
//	public class Book{
//		// 생성자 없음.
//		// => 컴파일러가 기본 생성자 자동 생성.
//	}
//	// 자동 생성 된 기본 생성자로 객체 생성 가능.
//	Book book = new Book();
}

//생성자를 통한 객체 생성
//		Car myCar = new Car("Red", "Automatic", "Gasoline");
	
////// 레퍼런스 변수
////Person hong;
////// new 연산자로 객체 생성
////hong = new Person();
//
//// 레퍼런스 변수 선언과 객체 생성 한번에 가능!
//Person hong = new Person();
//
//// 객체의 멤버에 접근
//hong.setName("홍길동");
//System.out.println(hong.getName() + " " + hong.walk());
//}