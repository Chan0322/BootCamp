package com.study;

public class Human {
	static int humanAge(int age) {
		age = 25;  // 매개변수의 값만 변경한다.
		return age;
	}
	
	public static void main(String[] args) {
		int age = 15;
		humanAge(age);
		System.out.println(age);	// 원본 값 15
		System.out.println(humanAge(age));	// 매개변수의 값 25
	}
}
