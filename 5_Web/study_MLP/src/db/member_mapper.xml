<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mlp.member">
	<select id="selectOne" resultType="MemberDto">
		SELECT *
		FROM MLPMEMBER
		WHERE MEMID=#{memid} AND MEMPW=#{mempw} AND MEMENABLED='Y'
	</select>
	
	<update id="update">
		UPDATE MLPMEMBER
		SET MEMNAME=#{memname}, MEMEMAIL=#{mememail}, MEMPHONE=#{memphone}, MEMPW=#{mempw}
		WHERE MEMNO=#{memno}
	</update>
	
	<select id="selectOneByMemno" resultType="MemberDto">
    	SELECT *
    	FROM MLPMEMBER
    	WHERE MEMNO = #{memno}
	</select>
	
</mapper>